import { HoneymoonPackages } from './honeymoon-packages.js';

describe('HoneymoonPackages', () => {
  it('should create an instance', () => {
    expect(new HoneymoonPackages()).toBeTruthy();
  });
});
