import { SoloTravelerPackages } from './solo-traveler-packages.js';

describe('SoloTravelerPackages', () => {
  it('should create an instance', () => {
    expect(new SoloTravelerPackages()).toBeTruthy();
  });
});
